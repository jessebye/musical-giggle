version: v1.0
name: Deploy
blocks:
  - name: Deploy to Stage Kubernetes Cluster
    task:
      jobs:
        - name: Update and commit manifest
          commands:
            - echo "updated the manifest"
agent:
  machine:
    type: e1-standard-2
    os_image: ubuntu1804
promotions:
  - name: Deploy to Prod
    pipeline_file: pipeline_3.yml
    auto_promote:
      when: branch = 'master' AND result = 'passed'
