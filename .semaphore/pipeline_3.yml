version: v1.0
name: Prod Deploy
blocks:
  - name: Deploy to Prod Kubernetes Cluster
    task:
      jobs:
        - name: Update and commit manifest
          commands:
            - echo "updated a manifest"
agent:
  machine:
    type: e1-standard-2
    os_image: ubuntu1804
